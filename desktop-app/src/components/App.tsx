import { Typography } from "@mui/material";
import * as React from "react";

export const App: React.FC = () => (
  <Typography variant="h1" color="primary.main" bgcolor="background.default">
    Hello from MUI!
  </Typography>
);
